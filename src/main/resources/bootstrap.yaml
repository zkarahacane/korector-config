spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: ${GIT_URI:https://github.com/zkarahacane/korector-config-file.git}
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}